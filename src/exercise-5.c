#include <stdio.h>

int x;
int y;
int max;

int main(void)
{
    scanf("%d",&x);
    scanf("%d",&y);

    if (x <= y);
    max = y;
    else
    max = x;

    printf("The maximum is %d.\n", max);

    return 0;

}